name: "[Cronicas] CI/CD"

on:
  push:
    branches: 
      - main

jobs:
  ci:
    name: CRONICAS CI
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4